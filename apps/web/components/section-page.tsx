import {
  ShellPage,
} from "@/components/shell/shell-screen-primitives";

import type { SectionKey } from "@/lib/navigation";

export function SectionPage({ sectionKey: _sectionKey }: { sectionKey: SectionKey }) {
  return (
    <ShellPage className="max-w-7xl gap-6 py-6">
      {null}
    </ShellPage>
  );
}
